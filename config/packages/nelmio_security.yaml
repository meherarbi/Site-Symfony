nelmio_security:
    # prevents framing of the entire site
    clickjacking:
        paths:
            '^/.*': DENY

    # disables content type sniffing for script resources
    content_type:
        nosniff: true

    csp:
        hosts: []
        content_types: []
        enforce:
            level1_fallback: false
            browser_adaptive:
                enabled: false
            report-uri: '%router.request_context.base_url%/nelmio/csp/report'
            default-src:
                - 'https://foryoumer.tn/assets/fonts/'
            img-src:
                - 'self'
                - https://foryoumer.tn/uploads/
                - https://foryoumer.tn/assets/images/
    
            connect-src:
                - 'https://foryoumer.tn/_wdt/'

            font-src: ['https://foryoumer.tn/']    
            script-src:
                - 'self'
                - 'unsafe-inline'
                - https://code.jquery.com/jquery-3.6.0.min.js
                - https://cdnjs.cloudflare.com/ajax/libs/jquery/3.3.1/jquery.min.js
            style-src: ["'self'", "'unsafe-inline'", "https://foryoumer.tn/assets/css/style.css", "https://foryoumer.tn/assets/css/custom_bootstrap.css", "https://foryoumer.tn/assets/css/normalize.css", "https://foryoumer.tn/assets/css/fontawesome.css", "https://foryoumer.tn/assets/css/elegant.css", "https://foryoumer.tn/assets/css/animate.css", "https://foryoumer.tn/assets/css/slick.css", "https://foryoumer.tn/assets/css/scroll.css", "https://foryoumer.tn/assets/css/icomoon.css", "https://foryoumer.tn/assets/css/jquery.fancybox.min.css", "https://foryoumer.tn/assets/css/mystore.css", "https://foryoumer.tn/assets/css/select2.min.css", "https://foryoumer.tn/build/css/intlTelInput.css"]
            block-all-mixed-content: true # defaults to false, blocks HTTP content over HTTPS transport
            # upgrade-insecure-requests: true # defaults to false, upgrades HTTP requests to HTTPS transport

   

    # forces Microsoft's XSS-Protection with
    # its block mode
    xss_protection:
        enabled: true
        mode_block: true

    # Send a full URL in the `Referer` header when performing a same-origin request,
    # only send the origin of the document to secure destination (HTTPS->HTTPS),
    # and send no header to a less secure destination (HTTPS->HTTP).
    # If `strict-origin-when-cross-origin` is not supported, use `no-referrer` policy,
    # no referrer information is sent along with requests.
    referrer_policy:
        enabled: true
        policies:
            - 'no-referrer'
            - 'strict-origin-when-cross-origin'
